# Contributing

1. Create a feature branch.
2. Add or update tests.
3. Run `python -m pytest -v`.
4. Do not commit `.env`, secrets, PII, or generated audit logs.
5. Open a pull request with validation evidence.
